import { Compiler, WebpackPluginInstance } from "webpack";
interface Option {
    manifestJSONPath: string;
    privateKeyPath: string;
    pluginZipPath: string | PluginZipPathFunction;
}
declare type PluginZipPathFunction = (id: string, manifest: {
    [key: string]: any;
}) => string;
declare class KintonePlugin implements WebpackPluginInstance {
    private readonly options;
    private readonly name;
    private privateKey;
    constructor(options?: Partial<Option>);
    apply(compiler: Compiler): void;
    /**
     * Generate a plugin zip
     */
    private generatePlugin;
}
export default KintonePlugin;
